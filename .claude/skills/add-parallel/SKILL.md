---
name: add-parallel
description: "Redirected — this skill is now handled by the orchestrator."
---

This skill has moved to the orchestrator. Capability skills are now deployed as capability pods or channel extensions via the orchestrator's skill system.

To add this capability at runtime, use the admin shell or send `/add-parallel` to the main group.
