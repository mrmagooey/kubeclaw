---
name: add-telegram
description: "Redirected — this skill is now handled by the orchestrator."
---

This skill has moved to `skills/channel/telegram.md` (channel self-configuration) and `skills/orchestrator/channel-setup.ts` (K8s resource creation).

To add this channel at runtime, send `/add-telegram` to the main group or use the admin shell `setup_channel` tool.
