---
name: add-signal
description: "Redirected — this skill is now handled by the orchestrator."
---

This skill has moved to `skills/channel/signal.md` (channel self-configuration) and `skills/orchestrator/channel-setup.ts` (K8s resource creation).

To add this channel at runtime, send `/add-signal` to the main group or use the admin shell `setup_channel` tool.
